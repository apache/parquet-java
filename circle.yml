dependencies:
  override:
  - ./scripts/ci-build.sh
  pre:
  - mkdir /home/ubuntu/.aws
machine:
  java:
    version: oraclejdk8
  post:
  - /home/ubuntu/rms-one-deployment/scripts/ci-unlock
test:
  override:
  - echo "tests complete."
